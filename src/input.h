#ifndef INPUT_
#define INPUT_
#include "datafile.h"
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
int read_input(mdsys_t *sys,int *nprint, char *restfile, char *trajfile, char *ergfile, char *line);
#endif
